# StoryTable User Manual

`StoryTable-User-Manual.pdf` is the stable public copy linked from
https://www.storytable.app/manual. The release workflow replaces it only with a
valid PDF whose edition matches the newest published StoryTable desktop build.

`current-version.txt` records the published manual edition for simple checks.
